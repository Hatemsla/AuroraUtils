name: test_ffi_plugin
description: "A new Flutter project."
publish_to: 'none'

environment:
  sdk: '>=3.2.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  ffi_plugin:
    path: plugins/ffi_plugin
  
  test_mavlink:
    path: plugins/test_mavlink

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true